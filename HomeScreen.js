import React, {useState} from "react";
import {
    View,
    Text,
    FlatList,
    Button,
    ActivityIndicator,
    StyleSheet,
} from "react-native";

const HomeScreen = (props) => {
    return (
        <View>
            <Text>Home</Text>
        </View>
    );
}
